Call `start_presentation()` first and follow the role document it returns.
The user's first message may begin with `Interaction mode: dialogue|fast` or `Continued from: …` — the role document explains both.
